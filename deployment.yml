apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication-microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authentication-microservice
  template:
    metadata:
      labels:
        app: authentication-microservice
    spec:
      containers:
      - name: authentication-microservice
        image: 192.168.1.2:5000/authentication-microservice
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: authentication-microservice
spec:
  ports:
  - port: 80
    nodePort: 30111
  selector:
    app: authentication-microservice
  type: NodePort
